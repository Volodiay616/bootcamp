<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_promote_tree_view" model="ir.ui.view">
        <field name="name">Products Promote</field>
        <field name="model">sale.product.promote</field>
        <field name="inherit_id" ref="product.product_product_tree_view" />
        <field name="priority">99</field>
        <!-- <field name="arch" type="xml">
            <field name="order_id" position = "after">
                <field name="line_number" />
            </field>               
        </field> -->
    </record>

    <record id="sale_product_promote_action" model="ir.actions.act_window">
        <field name="name">Sale Product Promote</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">sale.product.promote</field>
        <field name="view_id" ref="product_promote_tree_view"/>
        <!-- <field name="view_id" ref="product_promote_tree_view"/> -->
        <field name="view_mode">kanban,tree,form,activity</field>
    </record>
    
    <menuitem id="menu_product_promote"
        name="Products Promote"
        parent="sale.product_menu_catalog"
        action="sale_product_promote_action"
        sequence="4"
        groups="sales_team.group_sale_salesman"/>

</odoo>
